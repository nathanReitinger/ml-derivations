// Kept in sync with /VERSION (the single source of truth for the current
// version number - check that file to know what to bump next). Bump both
// every time you push a change, in the same commit as the change. The
// topbar shows this (id="build-badge"), so after deploying you can
// hard-refresh the live site and confirm the version you just pushed is
// actually the one showing - proving a deploy landed, independent of
// whatever a CDN/browser cache thinks is current.
window.BUILD_ID = 'v2.2 · 2026-09-14';
