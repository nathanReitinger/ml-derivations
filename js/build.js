// Bump this one line every time you push a change, then commit it in the
// same commit as the change. The topbar shows it (id="build-badge"), so
// after deploying you can hard-refresh the live site and confirm the
// number you just pushed is actually the one showing — that's the whole
// point: proving a deploy landed, independent of whatever a CDN/browser
// cache thinks is current.
window.BUILD_ID = 'build-2 · 2026-09-13';
